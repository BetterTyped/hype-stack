packages:
  - "apps/*"
  - "packages/*"
  - "shared/*"
  - "libs/*"

# pnpm blocks lifecycle scripts by default. Electron's postinstall downloads the
# platform binary, without it `electron-forge start` cannot find an executable.
onlyBuiltDependencies:
  - electron
